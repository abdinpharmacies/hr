<odoo>
    <record id="view_conflict_form" model="ir.ui.view">
        <field name="name">ab_deploy_conflict_form</field><field name="model">ab_deploy_conflict</field>
        <field name="arch" type="xml"><form string="Deployment Queue Conflicts">
            <field name="request_id"/><field name="has_hidden_conflicts" invisible="1"/><field name="can_replace" invisible="1"/><field name="changed" invisible="1"/>
            <div class="alert alert-warning" role="alert" invisible="not changed">The selected servers or conflicting executions changed. Review the selection and updated list before confirming again.</div>
            <p>These servers already have waiting or unfinished deployments. You can cancel older waiting executions on the same servers. Running or uncertain scripts and other servers are not stopped.</p>
            <p invisible="not has_hidden_conflicts">Other deployments are using these servers. Their details are restricted. You can queue after them.</p><field name="conflict_ids"><list create="0" edit="0" delete="0"><field name="request_id"/><field name="server_id"/><field name="state"/><field name="stage"/></list></field>
            <footer>
                <button name="action_replace" type="object" invisible="not can_replace" string="Cancel Older Waiting Executions and Queue" class="btn-primary"/>
                <button name="action_wait" type="object" string="Keep Existing and Queue After"/>
                <button string="Go Back" special="cancel"/>
            </footer>
        </form></field>
    </record>
</odoo>
