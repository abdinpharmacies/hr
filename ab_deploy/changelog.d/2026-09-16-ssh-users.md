# Allow configured SSH users

Commit: uncommitted (new module; no prior module commits)
Author: emadco88
Date: 2026-09-16 (UTC)
Original commit subject: not applicable; no commit created

## Current changes before commit:

- Accept any SSH user resolved by the configured alias, including root, without an additional setting.
- Remove the root restriction from the legacy SSH User field while retaining SSH port validation.
- Document root SSH support and distinguish it from the configured Odoo OS user, which must remain non-root.
- Preserve approval checks, SSH host-key verification, batch authentication and execution tracking.
- No new or modified translatable UI strings; existing Arabic message entries are preserved in both catalogs.

## Validation

- Mocked connection checks passed for root, deploy and odoo19; private connection details remain excluded from results.
- Invalid aliases were rejected before invoking SSH; batch authentication and strict host-key verification remain enabled.
- Isolated port constraint checks accepted valid ports and rejected 0 and 65536, including with root selected.
- Python syntax and diff whitespace checks passed; the separate Odoo OS root restriction remains present.
- No live SSH connections, service restarts or database upgrades performed. Runtime rollout is left to the user.

Files changed:

- `ab_deploy/runner/service.py`
- `ab_deploy/models/deployment.py`
- `ab_deploy/runner/README.md`
- `ab_deploy/changelog.d/2026-09-16-ssh-users.md`
